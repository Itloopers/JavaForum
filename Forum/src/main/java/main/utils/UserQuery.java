package main.utils;

public abstract class UserQuery {

	/*
	 * Query form UserRegistration isUserExist()
	 * 
	 * and
	 * 
	 * Query from DBUserAccounts getUserAccount()
	 */

	/*
	 * UserAccount user = new UserAccount(); EntityManager entityManager =
	 * ConnectionToDB.entityManager; CriteriaBuilder criteriaBuilder =
	 * entityManager.getCriteriaBuilder(); CriteriaQuery<UserAccount> createQuery =
	 * criteriaBuilder.createQuery(UserAccount.class); Root<UserAccount>
	 * fromUserAccount = createQuery.from(UserAccount.class); Path<Object>
	 * usernameField = fromUserAccount.get("username");
	 */

}
